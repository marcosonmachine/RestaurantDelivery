//: next js socks for cart count
// Tried to implement socks but can't due to it's next + I am lazy 
// Not just sock even a only server api for this will be good since multiple component at once require it
// Idea of a NAT/Some intersecting group having some base for a server. Sub servers and one server. 